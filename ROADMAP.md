# Roadmap

This roadmap is status-based. The detailed execution log lives in `.codex/plans/browser-provider-mcp-deprecation-plan.md` and `docs/plans/2026-05-29-browser-provider-mcp-deprecation-plan.md`. The latest completion / documentation coverage audit lives in `docs/status/2026-06-09-completion-and-docs-audit.md`. The 2026-06-10 refactor audit lives in `docs/status/2026-06-10-refactor-audit.md`; rollout 8 follow-up planning lives in `docs/runtime/native-web-fallback-dispatch-contract.md`, `docs/plans/2026-06-12-source-dispatch-decomposition-plan.md`, and `docs/status/2026-06-12-code-audit-triage.md`; rollout 9 collector redaction closure lives in `docs/status/2026-06-12-multi-agent-rollout-9.md`; rollout 10 default fallback helper closure lives in `docs/status/2026-06-12-multi-agent-rollout-10.md`; rollout 11 Chrome launcher / legacy alias / Source Dispatch S1 closure lives in `docs/status/2026-06-13-multi-agent-rollout-11.md`. The 2026-06-15 progress review / code-review correction (rollout 1-11 status reconciliation, audit-finding re-verification, and three open corrections) lives in `docs/status/2026-06-15-progress-review-and-correction.md`. The 2026-06-22 rollout 11 final-validation closure and project progress assessment lives in `docs/status/2026-06-22-rollout-11-closure-and-progress-assessment.md`.

> **2026-06-12 refactor note**: `module_hooks.py` has been split into a domain package (`module_hooks/`), the 160 `_is_*_request` static methods have been extracted into `_NativeWebRequestMatchers` mixin, and `apply_minimal_protection` dispatch has progressed through domain extraction (`_dispatch_closure_prefix`, `_dispatch_heap`, `_dispatch_object_graph`, `_dispatch_paused_session`, `_dispatch_closure_runtime`, `_dispatch_module_federation`, `_dispatch_custom_loader`, `_dispatch_async_chunk`, `_dispatch_module_tail`, `_dispatch_observation_review`, `_dispatch_recursive_continuation_readiness`). After rollout 10, `apply_minimal_protection` is down to a thin dispatcher with 0 direct request branch predicates, and the terminal fallback hook install / snapshot behavior has moved into `_dispatch_default_hook_fallback(...)` under the reviewed fallback dispatch contract. The next refactor target is no longer B3c branch extraction or fallback extraction, but the larger `_dispatch_source(...)` staged decomposition. The heap retained-size executor MVP, path-to-root executor MVP, and constructor-growth drilldown analysis MVP listed under "Done" below are all implemented with V8 snapshot parsing and bounded estimates; they do not claim proof or complete traversal. Items listed under "Still not fully closed" refer to automation, raw-heap proof paths, and complete binding semantics — not to the MVP executors which are shipped.

> **2026-06-12 rollout 8 note**: fallback dispatch is now contract-documented before code extraction, `_dispatch_source(...)` has a staged decomposition plan instead of an ad-hoc rewrite, and the old readonly audit themes have been converted into tracked triage. The P0 native collector evidence redaction follow-up from that triage has since been closed by rollout 9.

> **2026-06-12 rollout 9 note**: native browser evidence redaction is now implemented for `StorageCollector` and `NetworkCollector` through `reverse_deepagent.browser.redaction`, with collector-level and downstream serialization guards proving raw cookie, Authorization, proxy auth, API key, CSRF, session, and token fixture values do not persist into snapshots or pipeline / fixture artifacts.

> **2026-06-12 rollout 10 note**: the default Native Web hook fallback is now extracted into `_dispatch_default_hook_fallback(...)` after the reviewed fallback contract. No-match fallback success, hook install failure, concrete-dispatch no-fallthrough, and provider/session failure outside fallback are covered by focused native-web tests. The next implementation priorities are staged `_dispatch_source(...)` decomposition, Chrome launcher hardening continuation, and active-doc legacy alias cleanup.

> **2026-06-13/22 rollout 11 note (evidence-backed)**: Chrome debug launcher parameter hardening now validates `DEBUG_PORT` and `WAIT_SECONDS`, keeps packaged/root scripts aligned, and documents `CHROME_PATH` versus `CHROME_APP_NAME` authority; active docs now clarify `mcp` / `jsreverser-mcp` as deprecated aliases rather than recommended runtime ids; `_dispatch_source(...)` S1 has moved the low-side-effect Source Map lookup, source-content, and bundler-symbol-scope review evidence branches into `native_web_source_dispatch.dispatch_source_map_review_evidence(...)`. Rollout 11 final validation completed 2026-06-22: `git diff --check` clean, `compileall` all modules, `unittest discover` 1765 tests OK (skipped=2). The next implementation priorities are Source Dispatch S2-S7, coordinator decoupling (B-1), and journal loader extraction (B-2) in separate review-gated PRs.

## Current direction

The project keeps the Web / JS reverse-engineering path as the mainline:

- `native-web + BrowserProvider + native collectors / hooks` is the canonical Web runtime path.
- `legacy-mcp` remains a compatibility backend and is supplied through the optional legacy MCP package.
- Browser implementations stay pluggable through `BrowserProvider` registrations instead of becoming coordinator dependencies.
- Delivery / review / rollback / lock workflows remain review-gated and side-effect explicit.
- Android, iOS, and mini-program full runtime chains remain deferred; only minimal adapter metadata, explicit tool probes, and platform-neutral artifact categories are maintained for now.

## Done / baseline shipped

These items have baseline implementations and tests. Some are intentionally conservative and remain review-only, dry-run-only, or explicit-approval-only.

- Runtime documentation governance baseline that splits the native-web capability description into implemented baseline, explicit reviewed execution surfaces, active Web-first gaps, and explicitly deferred automation / non-Web chains.

### Web runtime and browser provider baseline

- BrowserProvider contract and registry.
- Playwright Chromium provider.
- Remote CDP provider.
- CloakBrowser optional provider skeleton, launch / persistent-context / connect baseline, manual smoke path, and explicit workspace smoke evidence capture path.
- Browser provider doctor mode, side-effect-free provider matrix, `reverse-agent-browser-provider-smoke` workspace evidence CLI with metadata-only default / explicit launch smoke mode, redaction-safe requested provider config summaries / review command hints, side-effect-free `--print-launch-command` review helper, standalone `--review-smoke-json` acceptance review mode with `--minimum-evidence-level` / `--block-on-warnings` policy decision gate, dedicated `reverse-agent-browser-provider-smoke-policy` CI / PR gate wrapper, doctor-level side-effect-free `--print-browser-launch-command` review payload, and `reverse-agent-demo --browser-provider-smoke-json` attachment of reviewed existing smoke evidence into Web pipeline manifests with metadata-only `attachment_acceptance` plus nested acceptance report gating.
- BrowserProvider production readiness metadata baseline that classifies provider rows as `production-ready`, `review-required`, or `metadata-incomplete` without invoking provider factories, probing CDP, launching browsers, or calling MCP.
- Provider-specific production readiness rule catalog scaffold, currently covering Playwright Chromium, Remote CDP, CloakBrowser, hosted-CDP reference, Browserless CDP, Browserbase CDP, and AntiDetect hosted CDP lifecycle metadata contracts plus v5 required metadata keys for stealth, endpoint security, allocation lifecycle, profile persistence, and account-boundary policies without invoking provider factories or probing endpoints.
- Extensible BrowserProvider capability compatibility rule catalog for CDP/debugger/network/lifecycle plus proxy, humanize, mobile emulation, and extension capability checks.
- Functional external BrowserProvider fixture plugin package that proves entry-point discovery, metadata-only listing, delayed factory creation, and launch/connect smoke outside core runtime.
- Hosted CDP BrowserProvider template package that gives vendor anti-detect browsers, hosted browser services, and enterprise CDP brokers a provider-neutral external package seam with metadata-only registration and explicit Remote CDP contract smoke.
- Hosted CDP reference BrowserProvider package that models allocation / attach / release lifecycle, idempotent stop, redacted metadata, and launch smoke through the BrowserProvider contract without bundling a vendor SDK.
- Browserless CDP BrowserProvider package baseline with side-effect-free entry-point registration, HTTP DevTools endpoint delegation, direct browser WebSocket CDP Target/Page/Runtime support, secret-safe endpoint metadata, explicit smoke path, and provider-specific readiness rule coverage.
- Browserbase CDP BrowserProvider package baseline with side-effect-free entry-point registration, reviewed `connectUrl` attach, explicit Session API allocation on `start()`, direct browser WebSocket CDP Target/Page/Runtime support, secret-safe API / project / session / endpoint metadata, explicit smoke path, and provider-specific readiness rule coverage.
- AntiDetect hosted CDP BrowserProvider package baseline with side-effect-free entry-point registration, attach-only reviewed HTTP DevTools / browser WebSocket CDP endpoint support, review-gated vendor-neutral allocator contract on `start()`, redacted endpoint / allocation / profile metadata, vendor-neutral anti-detect policy metadata, and provider-specific readiness rule coverage.
- Managed Chrome debug launcher scripts and docs, including parameter validation for `DEBUG_PORT` / `WAIT_SECONDS`, packaged/root script parity, and documented `CHROME_PATH` versus `CHROME_APP_NAME` authority.
- MCP legacy downgrade, alias warnings, optional legacy MCP package split, and active-doc clarification that `mcp` / `jsreverser-mcp` are deprecated compatibility aliases rather than recommended runtime ids.

### Native Web collectors and hooks

- DOM, console, script inventory, navigation events, network metadata, request initiator, response body metadata, source cache, and WebSocket frame cache with hook fallback plus structured pre-subscription diagnostics; CDP WebSocket frames are post-attach only and historical replay is not claimed.
- Central browser evidence redaction helper plus default `StorageCollector` / `NetworkCollector` redaction for cookie, Authorization, proxy auth, API key, CSRF, session, token, and sensitive storage values, with collector and downstream serialization regression coverage.
- Fetch / XHR / cookie / WebSocket / anti-debug hook baselines, plus the extracted default Native Web hook fallback helper that preserves the existing `virtual://workspace/hook-timeline.json` terminal no-match behavior.
- Target-function wrapper and webpack-like module export hook baselines.
- Module discovery, runtime module cache / registry introspection, custom object runtime, module federation function-path candidate baseline, review-only module federation `get/init` plan baseline, review-gated module federation `init/get` probe baseline, review-gated remote factory invocation / export-summary baseline, review-only remote export hook selection plan baseline, review-approved remote export hook install baseline, review-only federation traversal graph / workflow plan, review-gated traversal workflow execution, review-only federation recursive traversal follow-up plan, review-gated federation recursive traversal follow-up checkpoint, and review-gated federation recursive traversal next-step execution baselines, read-only async chunk graph / loader metadata baseline, review-only custom-loader traversal plan baseline, bounded custom-loader traversal continuation planning baseline, review-only custom-loader traversal graph / queue baseline, review-only multi-step custom-loader traversal workflow plan baseline, review-gated custom-loader traversal workflow execution baseline, review-only bounded custom-loader traversal loop plan baseline, review-gated bounded custom-loader traversal loop execution baseline, review-only custom-loader recursive traversal follow-up plan baseline, review-gated custom-loader recursive traversal follow-up checkpoint baseline, review-gated custom-loader recursive traversal next-loop execution baseline, review-only custom-loader continuation workflow planning baseline, review-gated custom-loader continuation journal baseline, review-approved one-step custom-loader continuation execution baseline, side-effect-free custom-loader execution preflight baseline, review-approved single-step custom-loader execution result baseline, review-only custom-loader module diff / hook candidate refresh baseline, review-approved custom-loader module hook follow-through baseline, review-gated webpack async chunk load plan / explicit execution evidence baseline, review-only async chunk traversal graph / queue baseline, review-only async chunk traversal workflow plan baseline, review-gated async chunk traversal workflow execution baseline, review-only bounded async chunk traversal loop plan baseline, review-gated bounded async chunk traversal loop execution baseline, review-only async chunk recursive traversal follow-up plan baseline, review-gated async chunk recursive traversal follow-up checkpoint baseline, review-gated async chunk recursive traversal next-loop execution baseline, review-only async chunk module diff / hook candidate refresh baseline, and review-approved async chunk module hook follow-through baseline.
- Closure-scope function discovery baseline, review-only closure wrapper replacement planning baseline, review-only closure wrapper assignment safety proof baseline, review-only closure wrapper runtime mutability preflight baseline, review-approved same-process runtime mutability probe/result baseline with temporary assignment restore audit, opt-in same-process reviewed closure wrapper replacement execution MVP gated by that proof with optional runtime mutability result evidence gate and restore-plan artifact, closure wrapper strategy descriptor/catalog metadata where only `log-only-call-through` is install-supported and preview/mutation strategies remain plan-only, reviewed same-process closure wrapper restore execution baseline, read-only closure wrapper event harvesting artifact baseline, read-only closure wrapper continuation readiness descriptor baseline, review-only closure wrapper continuation execution plan descriptor baseline, review-gated one-iteration wrapper continuation execution baseline, read-only wrapper continuation follow-up checkpoint descriptor baseline, review-only wrapper continuation next-iteration plan descriptor baseline, and review-gated wrapper continuation next-iteration execution baseline; arbitrary automatic closure wrapper replacement remains unsupported.
- Source-level logpoints and Source Map exact / bias / sourceRoot / indexed-section remap baseline, with source-map `names` metadata, URL-like source equivalence, nested indexed-section stack metadata, extracted Source Dispatch S1 review-evidence helper for generated/original source-map lookup, source-map `sourcesContent` availability, and bundler-symbol-scope descriptors, review-only source-map readiness join descriptor for lookup / source-content / symbol-scope / fetch metadata, review-only source-map consumer action plan descriptor for debugger / source-logpoint / rebuild / hook review planning, review-only source-map consumer materialization descriptor for `reverse-deepagent.source-map-consumer-typed-review-payload.v1` typed debugger / source-logpoint / rebuild / hook review payloads with native-web metadata exposure, read-only source-map typed payload follow-through preflight descriptor for explicit debugger / source-logpoint / rebuild / hook review surfaces, review-only source-map follow-through review handoff descriptor, review-only source-map follow-through surface selection descriptor, review-only selected source-map executor-input review descriptor, review-only selected source-map executor approval / apply-plan descriptor, explicit-review-only selected source-map executor approval-record writer, read-only selected source-map executor apply-preflight descriptor, read-only source-map follow-through chain readiness descriptor, review-only Source Map follow-through one-step orchestration plan descriptor, read-only Source Map follow-through dispatch preflight descriptor, review-only Source Map follow-through dispatch approval / transaction plan descriptor, explicit-review-only Source Map follow-through dispatch approval-record writer, read-only Source Map follow-through dispatch transaction preflight descriptor, explicit-review-only Source Map follow-through dispatch transaction journal writer, read-only Source Map follow-through dispatch bounded executor gate descriptor, read-only Source Map follow-through dispatcher handoff descriptor, read-only Source Map follow-through dispatcher apply-preflight descriptor, explicit-review-only Source Map follow-through dispatcher result MVP, optional read-only dispatcher-result to selected-executor apply-preflight handoff audit, read-only selected-executor application handoff descriptor, read-only selected-executor result checkpoint descriptor, read-only follow-through completion / next-action checkpoint descriptor, read-only `source-map-terminal-review-package` Source Map terminal review package / audit handoff descriptor, read-only `source-map-terminal-review-closure-checkpoint` observed-result / closure audit checkpoint descriptor, read-only `source-map-terminal-review-final-audit` closure summary / final audit rollup descriptor, explicit-review-only Source Map terminal review action decision / result recorder baseline with raw-source input blocking, explicit-review-only selected Source Map source-logpoint install MVP, explicit-review-only selected Source Map debugger one-location application MVP, review-only Source Map debugger candidate descriptor with deterministic candidate ranking metadata, review-only Source Map debugger candidate selection handoff descriptor, review-only Source Map debugger candidate selected-input review helper, explicit-review-only selected Source Map hook function / module install MVP, review-only Source Map hook candidate refinement descriptor with deterministic candidate ranking metadata, review-only Source Map hook candidate selection handoff descriptor, review-only Source Map hook candidate selected-input review helper, explicit-review-only selected Source Map rebuild metadata application MVP, explicit-review-only selected Source Map reviewed rebuild bundle generation MVP, review-only bundler symbol scope descriptor, and review-gated credentialless external Source Map / indexed-section URL fetch metadata baseline.
- Page mutation audit, descriptor-safe scoped object-root mutation audit, review-only object graph diff, explicit scoped runtime-collected object graph diff, review-only CDP HeapProfiler heap snapshot readiness, explicit review-gated CDP HeapProfiler heap snapshot collect metadata MVP, review-only heap snapshot diff readiness pair-review descriptor, review-only heap snapshot diff executor preflight / raw-ingestion safety gate, review-only heap snapshot diff executor approval / transaction plan, explicit-review-only heap snapshot diff executor approval-record writer, read-only heap snapshot diff selected-analysis input preflight, read-only heap snapshot constructor-growth drilldown review descriptor, read-only heap snapshot retained-size / path-to-root preflight handoff descriptor, read-only heap snapshot retained-size executor input review / approval-gate descriptor, read-only heap snapshot retained-size approval / transaction plan descriptor, explicit-review-only heap snapshot retained-size approval-record writer, explicit-review-only heap snapshot retained-size estimate MVP, explicit-review-only heap snapshot path-to-root estimate MVP, explicit-review-only heap snapshot constructor-growth drilldown analysis MVP, and MutationObserver timeline baselines.
- Same-process paused-session continuation, durable inspect-only paused-session snapshots, read-only paused-session live-continuation preflight evidence, read-only paused-session target attach readiness proof, plan-only cross-process paused-session execution descriptor, reviewed cross-process Target.attachToTarget / Target.detachFromTarget probe baseline, read-only live callFrame recovery proof baseline, review-gated cross-process one-action resume / step / evaluate executor baseline, review-only next paused-event capture plan baseline, review-gated one-shot next paused-event capture execution baseline, review-only cross-process continuation checkpoint baseline, review-gated pre-action subscribe-and-action orchestration baseline, and review-only multi-step continuation workflow / journal plan baseline, and review-gated one-iteration multi-step continuation execution baseline, and read-only cross-process session lifecycle descriptor for same-process / durable / provided-artifact debugger state, including live-session / target / callframe / target-correlation / attachability / action-capability diagnostics, fresh paused-event requirements, retained attached-session requirements, one-action execution review gates, next paused-event capture planning gates, and captured-pause-to-next-reviewed-step checkpointing. The attach probe is explicit-review-only and does not enable Debugger, recover live callFrames, resume, step, evaluate, call MCP, or touch mobile runtimes; live callFrame recovery is read-only, requires fresh paused evidence after attach by default, one-action execution sends exactly one reviewed Debugger command without automatic continuation, next paused-event capture planning remains read-only without subscribing to `Debugger.paused`, next paused-event capture execution only subscribes / waits for one bounded event after explicit review without issuing another Debugger command, the continuation checkpoint only normalizes captured-pause evidence into the next review step without sending CDP, subscribing, recovering callFrames, executing actions, looping, calling MCP, or touching mobile runtimes, pre-action subscribe-and-action orchestration subscribes before exactly one reviewed resume / step command and captures at most one bounded paused event, and the multi-step continuation workflow / journal plan only composes bounded reviewed action steps plus checkpoint requirements without executing them, one-iteration continuation execution runs at most one reviewed planned step before requiring a checkpoint without looping, calling MCP, or touching mobile runtimes, the lifecycle descriptor only normalizes existing evidence without probing target liveness, recovering callFrames, subscribing to events, executing actions, or starting automation loops, the multi-step loop plan descriptor only composes existing lifecycle / workflow / execution / checkpoint evidence into the next reviewed loop iteration without executing or advancing the loop automatically, the review-gated multi-step loop execution baseline consumes that reviewed loop plan plus fresh live callFrame evidence to delegate exactly one next step to the existing continuation executor before requiring another checkpoint, the automatic-loop one-iteration / next-iteration MVP chain now includes read-only readiness / plan / preflight / approval / transaction / bounded-gate descriptors, explicit-review-only one-iteration execution, follow-up checkpointing, next-iteration planning / execution / follow-up checkpointing, a read-only following-iteration plan descriptor that reuses the existing next-iteration executor, a read-only bounded multi-iteration policy descriptor that normalizes budget / per-iteration review / checkpoint / fresh live callFrame gates, a read-only multi-iteration executor preflight descriptor that verifies future executor inputs, a review-only multi-iteration execution-plan descriptor, approval / transaction / journal / bounded-gate descriptors, the Step 264 explicit-review-only bounded multi-iteration executor MVP that executes at most one reviewed iteration before requiring checkpoint review, the Step 265 read-only multi-iteration execution follow-up checkpoint descriptor that normalizes that checkpoint / next-loop handoff, the Step 266 read-only multi-iteration next-step review plan descriptor that turns that handoff plus continuation checkpoint / loop plan / optional fresh callFrame evidence into the next explicit Step 264 executor review input, and the Step 267 read-only executor-input preflight descriptor that verifies the ready Step 266 handoff plus Step 264 journal / gate / loop plan / workflow / fresh live callFrame / retained session inputs while still blocking full automatic multi-iteration loops, the closure wrapper continuation execution plan descriptor only composes existing wrapper readiness plus paused-session lifecycle / loop / checkpoint / recovery evidence into a future review gate without executing wrapper-aware continuation, and the closure wrapper continuation follow-up checkpoint descriptor only composes post-execution wrapper events plus paused-session checkpoint evidence into the next reviewed wrapper-aware iteration without harvesting events, sending CDP, or advancing the loop automatically, and the wrapper continuation next-iteration plan descriptor turns that checkpoint plus the previous execution plan and paused-session loop plan into the next review input without recovering callFrames, executing actions, or advancing queues, and the wrapper continuation next-iteration execution baseline consumes that reviewed plan plus a fresh live callFrame to delegate exactly one next step to the existing continuation executor before requiring wrapper event harvesting and checkpoint review again.

### DeepAgents workspace and subagents

- DeepAgents workspace contract indexed-only baseline.
- Manifest-only workspace alias for future foldered virtual paths while keeping flat `workspace/*.json` paths canonical.
- Workspace artifact reader, specialized read-only review-helper artifact-ref resolver, rebuild generation artifact-ref inputs, delivery artifact-list resolver, delivery source compatibility audit, workspace migration readiness report, workspace consumer readiness score, opt-in dual-write expansion plan, opt-in expansion observed-result verifier / review workflow, narrow foldered-canonical migration pilot plan, read-only foldered-canonical migration preflight / rollback descriptor, review-only foldered-canonical migration apply plan descriptor, review-only foldered-canonical apply approval / transaction descriptor, read-only foldered-canonical manifest dry-run / rollback checkpoint descriptor, read-only foldered-canonical physical apply preflight descriptor, explicit-review-only foldered-canonical physical apply executor skeleton, read-only foldered-canonical post-apply validation descriptor, explicit post-apply validation result writer, read-only legacy fallback tightening readiness descriptor, review-only legacy fallback tightening apply plan descriptor, read-only legacy fallback tightening executor preflight descriptor, explicit-review-only legacy fallback tightening executor, read-only foldered-canonical finalization readiness descriptor, review-only foldered-canonical finalization plan descriptor, read-only foldered-canonical finalization preflight descriptor, explicit-review-only foldered-canonical finalization executor, read-only post-finalization audit descriptor, read-only broader rollout readiness descriptor, review-only broader rollout plan descriptor, read-only broader rollout preflight descriptor, explicit-review-only broader rollout executor, read-only broader rollout post-audit descriptor, review-only broader rollout rollback-vs-commit decision plan descriptor, explicit reviewed broader rollout decision record writer, explicit reviewed broader rollout commit executor, read-only broader rollout rollback preflight descriptor, explicit reviewed broader rollout rollback executor, limited dual-write pilot plan / scoped writer / result artifact / review workflow, pure-Python reviewed scoped dual-write pilot smoke CLI, resolver compatibility metrics, and workspace consumer adoption audit baselines for artifact key, legacy path, future path, `virtual://workspace/...` URI, artifact-root-relative consumption, explicit source_path classification, scored dual-write / foldered-canonical review readiness, reviewed expansion-scope planning, expansion output verification, foldered-canonical pilot planning, preflight file parity verification, apply-plan manifest / rollback guard review, approval / transaction / idempotency / validation review, manifest dry-run / rollback checkpoint review, physical apply preflight review, explicit physical apply execution with rollback checkpoint / journal / result evidence, post-apply validation review, post-apply validation result recording, legacy fallback tightening readiness review, legacy fallback tightening apply-plan review, legacy fallback tightening executor preflight review, explicit legacy fallback tightening execution with journal / result evidence, explicit foldered-canonical finalization execution with journal / result evidence, post-finalization transaction / journal / manifest audit review, broader rollout readiness review, broader rollout plan review, broader rollout preflight review, explicit broader rollout metadata execution with journal / result evidence, broader rollout post-audit transaction / journal / manifest verification, scoped dual-write output verification, and reviewed local delivery planning / apply inputs by coordinator, review / rebuild / timeline / hook / debugger / delivery subagents.
- Router, web recon, browser runtime, debugger, hook, timeline, review, rebuild, and delivery subagent baselines.
- Review approval ledger baseline.

### Flow timeline and review-gated materialization

- Flow timeline baseline with correlation hints, conservative correlation groups, manual stitch candidates, and review-gated stitch proposals.
- Auto-stitch dry-run scoring, policy decision gate, materialization plan, review-approved materializer skeleton, conflict resolver baseline, materialization audit / rollback writer, transaction log, rollback dry-run, review gate recompute, and final delivery package / transaction record baselines.
- Automatic stitching and automatic delivery remain disabled by design.

### Delivery, recovery, lock, and external delivery baselines

- Local delivery executor contract and manifest revision baseline.
- Backend artifact manifest mutation policy, in-place mutation preflight, explicit-review-only in-place mutation, recovery preflight, recovery apply, and cross-run transaction commit baselines.
- Delivery transaction inspector / doctor, transition executor, recovery executor, idempotency guard, rollback state writer, rollback preflight, and explicit-review-only rollback apply baselines.
- Delivery resume planner, resume runner, resume workflow scheduler, workflow journal, skipped-step journal context replay, workflow-local fencing propagation, journal-state fencing replay, lock lifecycle planning, lease renewal planning, workflow readiness plan, step dependency context matrix, and runtime-gate evidence projection.
- Delivery transaction lock provider contract with local-file, SQLite, and Redis baselines.
- ExternalDeliveryProvider contract and registry with local archive, webhook, presigned object, GitHub Release upload / reuse / duplicate preflight / overwrite-delete, explicit retry, idempotency ledger, optional provider template, S3-compatible external provider package baseline with reviewed HTTP PUT / presigned URL support, GitLab Release external provider package baseline that creates reviewed release records plus opt-in binary asset upload with inline-secret API URL blocking, upload URL safety checks, and secret-safe metadata, and internal artifact registry external provider package baseline with side-effect-free registration, dry-run planning, explicit apply approval, endpoint inline-secret blocking, metadata redaction, and mocked HTTP publication seam.

### Platform expansion boundary

- Android adapter interface draft and minimal `android-adb` backend metadata / explicit ADB probe / artifact export baseline.
- iOS adapter interface draft and minimal `ios-simulator` backend metadata / explicit `xcrun simctl` probe / artifact export baseline.
- Mini-program adapter interface draft and minimal `mini-program-devtools` metadata / configurable vendor-devtools probe / artifact export baseline.
- Platform-neutral artifact categories and `target_platforms` semantics.

### Strategy and rebuild quality baseline

- Generalized runtime context stability diff for legacy runtime payloads and provider-neutral strategy / rebuild consumers.
- Field-level runtime context classifications for stable, volatile, session-bound, missing, type-drift, and object-drift values.
- Secret-like runtime context previews redacted to type, length, and digest metadata while preserving legacy `stable_keys` / `volatile_keys` / `changes` compatibility fields.
- Generated rebuild review hints derived from runtime-context diff classifications for volatile, session-bound, missing, type-drift, and object-drift fields.
- Plan-only WASM / VM / obfuscation triage hook planner with protected-flow hook/debugger candidates and workspace artifact routes.
- Provider-neutral strategy evidence scoring baseline that combines detector confidence, validation status, replay URL presence, runtime-context stability, protected-flow triage, and rebuild readiness into review-only `evidence_score` payloads, plus conservative review-gate consumption of score labels / blockers for delivery decisions.
- StrategyDetector provider registry, external plugin template baseline, and reference StrategyDetector provider package baseline through the `reverse_deepagent.strategy_detectors` entry point; metadata listing is side-effect-free and does not run detectors, collect runtime context, execute replay, start browsers, call MCP, or touch mobile runtimes. The reference provider performs deterministic marker inventory over an explicitly provided source string and records redacted marker metadata without raw source snippets.

## Active non-mobile follow-ups

These are the next realistic non-mobile work items. They should be implemented without leaking provider-specific details into the coordinator.

### Documentation and governance

- Keep `ROADMAP.md`, `.codex/plans/`, README, AGENTS, and runtime docs aligned after each architecture-level change.
- Keep long runtime and delivery status sections split into implemented baseline, active remaining work, and explicitly deferred automation as new architecture-level changes land.

### Browser / CDP / hook depth

- Additional real third-party BrowserProvider plugins beyond the Browserless CDP, Browserbase CDP, AntiDetect hosted CDP allocator-contract baseline, functional fixture, hosted-CDP template, and hosted-CDP reference package baselines, such as vendor-specific anti-detect SDK integrations or other hosted browser services; new providers should preserve explicit metadata-only registration, fill the production readiness metadata contract, and provide `workspace/browser-provider-smoke.json` evidence that passes the standalone policy gate before runtime smoke is accepted.
- Additional provider-specific compatibility / readiness rules when real third-party provider plugins introduce new capability flags, lifecycle policies, vendor account constraints, or smoke-evidence requirements beyond the built-in v5 required-metadata baseline.
- Cross-process live CDP paused execution continuation beyond the read-only live-continuation preflight, target attach readiness proof, plan-only cross-process execution descriptor, reviewed attach/detach probe, read-only live callFrame recovery proof, review-gated one-action resume / step / evaluate executor, review-only next paused-event capture plan, review-gated one-shot next paused-event capture execution, review-only cross-process continuation checkpoint, review-gated pre-action subscribe-and-action orchestration, and review-only multi-step continuation workflow / journal plan, review-gated one-iteration multi-step continuation execution, read-only cross-process session lifecycle, review-only multi-step loop plan, review-gated multi-step loop one-iteration execution, read-only / review-only automatic-loop readiness descriptor, review-only / plan-only automatic-loop execution-plan descriptor, read-only / review-only / preflight-only automatic-loop executor preflight descriptor, review-only / approval-plan-only / transaction-plan-only automatic-loop executor approval-plan descriptor for a future bounded executor contract, and explicit-review-only automatic-loop executor approval-record writer, read-only automatic-loop transaction / journal preflight descriptor, explicit-review-only automatic-loop transaction journal writer, read-only automatic-loop bounded executor gate descriptor, read-only / review-only automatic-loop next-iteration plan descriptor, explicit-review-only automatic-loop next-iteration execution MVP, read-only automatic-loop next-iteration follow-up checkpoint descriptor, read-only / review-only automatic-loop following-iteration plan descriptor, read-only / review-only / policy-only automatic-loop bounded multi-iteration policy descriptor, read-only / review-only / preflight-only automatic-loop multi-iteration executor preflight descriptor, review-only / plan-only automatic-loop multi-iteration execution-plan descriptor, review-only / approval-plan-only automatic-loop multi-iteration executor approval / transaction plan descriptor, explicit-review-only multi-iteration executor approval-record writer, read-only / review-only / transaction-preflight-only automatic-loop multi-iteration transaction preflight descriptor, explicit-review-only automatic-loop multi-iteration transaction journal writer, review-only closure wrapper continuation execution plan, review-gated closure wrapper continuation one-iteration execution, read-only closure wrapper continuation follow-up checkpoint, and review-only closure wrapper continuation next-iteration plan, and review-gated closure wrapper continuation next-iteration execution baselines; automatic multi-step action loops are now supported only as an explicit-review-only bounded one-iteration native-web MVP followed by a read-only automatic-loop follow-up checkpoint descriptor, read-only next-iteration plan descriptor, explicit-review-only next-iteration execution MVP, read-only next-iteration follow-up checkpoint descriptor, read-only following-iteration plan descriptor, and read-only bounded multi-iteration policy descriptor, read-only multi-iteration executor preflight descriptor, review-only multi-iteration execution-plan descriptor, review-only multi-iteration executor approval / transaction plan descriptor, explicit-review-only multi-iteration executor approval record writer, read-only multi-iteration transaction preflight descriptor, explicit-review-only multi-iteration transaction journal writer, read-only multi-iteration bounded executor gate, Step 264 one-reviewed-iteration multi-iteration executor MVP, Step 265 read-only multi-iteration execution follow-up checkpoint, Step 266 read-only next-step review plan, and Step 267 read-only executor-input preflight after the readiness / execution-plan / executor-preflight / approval-plan / approval-record / transaction-preflight / transaction-journal / bounded-gate / execution chain; full automatic bounded multi-iteration loops beyond that one-reviewed-iteration MVP plus review-only follow-up / next-step / executor-input handoff, automatic wrapper continuation loops, automatic live callFrame recovery, automatic queue advance, and longer-lived cross-process session management remain capability-gated follow-ups.
- Deeper execution-style custom-loader traversal beyond one reviewed recursive next-loop checkpoint and the current bounded continuation planning, review-only traversal graph / queue, review-only multi-step traversal workflow plan, review-gated traversal workflow execution, review-only bounded traversal loop planning, review-gated bounded traversal loop execution, review-only recursive traversal follow-up planning, review-gated recursive traversal follow-up checkpointing, review-gated recursive next-loop execution, review-only continuation workflow planning, review-gated continuation journal, review-approved one-step continuation execution, review-approved single-step custom-loader execution, module-diff refresh, module hook follow-through, and unified read-only recursive continuation readiness descriptor baselines remains capability-gated for deeper recursive traversal. Deep async chunk traversal beyond the current review-only traversal graph / queue, review-only traversal workflow plan, review-gated one-step traversal workflow execution, review-only bounded traversal loop planning, review-gated bounded loop execution, review-only recursive traversal follow-up planning, review-gated recursive follow-up checkpointing, review-gated recursive next-loop execution, review-gated webpack ensure, review-only async chunk module diff / hook candidate refresh, review-approved async chunk module hook follow-through, and the same unified readiness descriptor remains capability-gated for deeper multi-iteration traversal.
- Deeper recursive federation traversal and remote-module analysis beyond the current review-only traversal graph / workflow plan, review-gated one-step traversal workflow execution, review-only recursive follow-up planning, review-gated recursive follow-up checkpointing, review-gated recursive next-step execution, review-only recursive continuation journal / multi-step checkpoint planning, review-gated recursive continuation checkpoint execution, reviewed factory invocation / export-summary, review-only export-hook-plan, and review-approved remote export hook install baselines.
- Closure wrapper replacement hardening beyond the current review-only assignment safety proof, runtime mutability preflight, review-approved runtime mutability probe/result, strategy descriptor/catalog metadata, and same-process `log-only-call-through` reviewed install / restore execution baselines plus the read-only wrapper-continuation readiness descriptor, review-only wrapper-continuation execution plan descriptor, review-gated one-iteration wrapper continuation execution baseline, read-only wrapper-continuation follow-up checkpoint descriptor, review-only next-iteration plan descriptor, and review-gated next-iteration execution baseline, including arbitrary closure-local wrapper support, reviewed executors for non-`log-only-call-through` strategies, automatic wrapper continuation loops, and cross-process live wrapper execution integration.
- Full source-map consumer semantics beyond the current local remap, review-only source-map lookup descriptor, review-only source-map source-content availability descriptor, review-only source-map readiness join descriptor, review-only source-map consumer action plan descriptor, review-only source-map consumer materialization descriptor with stable typed review payload metadata, read-only source-map typed payload follow-through preflight descriptor, review-only source-map follow-through review handoff descriptor, review-only source-map follow-through surface selection descriptor, review-only selected source-map executor-input review descriptor, review-only selected source-map executor approval / apply-plan descriptor, explicit-review-only selected source-map executor approval-record writer, read-only selected source-map executor apply-preflight descriptor, read-only source-map follow-through chain readiness descriptor, review-only Source Map follow-through one-step orchestration plan descriptor, explicit-review-only selected Source Map source-logpoint install MVP, explicit-review-only selected Source Map debugger one-location application MVP, review-only Source Map debugger candidate descriptor with deterministic candidate ranking metadata, review-only Source Map debugger candidate selection handoff descriptor, review-only Source Map debugger candidate selected-input review helper, explicit-review-only selected Source Map hook function / module install MVP, review-only Source Map hook candidate refinement descriptor with deterministic candidate ranking metadata, review-only Source Map hook candidate selection handoff descriptor, review-only Source Map hook candidate selected-input review helper, explicit-review-only selected Source Map rebuild metadata application MVP, explicit-review-only selected Source Map reviewed rebuild bundle generation MVP, review-only bundler symbol scope descriptor, and review-gated credentialless URL fetch metadata baseline; current lookup / source-content / readiness / action-plan / materialization / typed-payload-preflight / followthrough-review / surface-selection / selected-executor-input-review / selected-executor-approval-plan / selected-executor-approval-record / selected-executor-apply-preflight / chain-readiness / one-step-plan / dispatch-preflight / dispatch-approval-plan / dispatch-approval-record / dispatch-transaction-preflight / dispatch-transaction-journal / dispatch-bounded-executor-gate / dispatcher-handoff / dispatcher-apply-preflight / dispatcher-result / dispatcher-result-to-selected-apply-preflight handoff / selected-executor-application-handoff / selected-executor-result-checkpoint / followthrough-completion-checkpoint / `source-map-terminal-review-package` / `source-map-terminal-review-closure-checkpoint` / `source-map-terminal-review-final-audit` / symbol-scope outputs are conservative review or audit artifacts, while source-map source-logpoint installation, one-location debugger breakpoint application, reviewed function / module hook installation, digest-only rebuild metadata application, and reviewed rebuild bundle generation from successful metadata plus reviewer-supplied TaskCard / FinalResult inputs are supported only as explicit-reviewed applications over ready review gates. Automatic debugger continuation / live callFrame recovery, automatic hook installation without explicit review, automatic Source Map follow-through, automatic raw-source-aware rebuild generation from Source Map metadata, automatic source-logpoint installation without review, complete binding / lexical scope resolution, and raw source text / preview export remain unsupported follow-ups; reviewed debugger candidate selection handoff into selected-executor-input-review context, selected-input review packaging from that handoff, reviewed hook candidate selection handoff into selected-executor-input-review context, selected-input review packaging from that hook handoff, and chain readiness normalization for the current follow-through stage / next explicit review action and one-step explicit-review plan packaging plus dispatch preflight allowlist / input readiness plus approval / transaction plan readiness, explicit dispatch approval-record writing, read-only dispatch transaction preflight, explicit dispatch transaction journal writing, read-only bounded dispatch gate, read-only dispatcher handoff, read-only dispatcher apply-preflight, explicit-review-only dispatcher result decision recording, optional dispatcher-result handoff verification inside selected executor apply-preflight, read-only selected-executor application handoff packaging, read-only selected-executor result checkpointing, read-only follow-through completion / next-action checkpointing, and read-only `source-map-terminal-review-package` terminal review package / audit handoff packaging, read-only `source-map-terminal-review-closure-checkpoint` observed-result closure audit checkpointing, and read-only `source-map-terminal-review-final-audit` final audit rollup are supported as triage / audit surfaces only; selected executor execution and executing any recommended terminal review action remain separate explicit follow-ups, while terminal review action decision / result recording is now available as an explicit-review-only baseline.
- Full JS heap diff beyond the current descriptor-safe scoped object-root mutation audit, review-only object graph diff descriptor, explicit scoped runtime-collected object graph diff baseline, review-only `heap-snapshot-readiness` preflight baseline, explicit review-gated `heap-snapshot-collect` digest-only metadata MVP, review-only `heap-snapshot-diff-readiness` pair-review descriptor, review-only `heap-snapshot-diff-executor-preflight` raw-ingestion safety gate, review-only `heap-snapshot-diff-executor-approval-plan` approval / transaction plan, explicit-review-only `heap-snapshot-diff-executor-approval-record` writer, read-only `heap-snapshot-diff-executor-transaction-preflight` descriptor, explicit-review-only `record_heap_snapshot_diff_executor_transaction_journal` writer, read-only `heap-snapshot-diff-executor-bounded-gate` descriptor, and explicit-review-only `heap-snapshot-diff-executor-result` summary-diff executor MVP, and read-only `heap-snapshot-diff-followup-checkpoint` result follow-up checkpoint plus read-only `heap-snapshot-diff-selected-analysis-input-preflight` selected follow-up input preflight plus read-only `heap-snapshot-constructor-growth-drilldown` constructor-growth review descriptor plus read-only `heap-snapshot-retained-path-preflight` retained-size / path-to-root handoff descriptor plus read-only `heap-snapshot-retained-size-input-review` retained-size executor input review / approval-gate descriptor plus read-only `heap-snapshot-retained-size-approval-plan` retained-size approval / transaction plan descriptor plus explicit-review-only `record_heap_snapshot_retained_size_approval` approval-record writer plus read-only `heap-snapshot-retained-size-transaction-preflight` transaction preflight descriptor plus explicit-review-only `record_heap_snapshot_retained_size_transaction_journal` transaction journal writer plus read-only / review-only `heap-snapshot-retained-size-bounded-gate` bounded executor gate descriptor; `object-graph-diff` still consumes caller-provided snapshots only, `runtime-object-graph-diff` collects only one strict dotted object root with bounded descriptor-safe snapshots, `heap-snapshot-readiness` only normalizes provided BrowserProvider / CDP / HeapProfiler capability evidence plus size / redaction gates, `heap-snapshot-collect` sends one reviewed HeapProfiler snapshot command sequence only to emit digest / byte-count / chunk-count metadata, `heap-snapshot-diff-readiness` only verifies two collect metadata descriptors before a future diff executor, `heap-snapshot-diff-executor-preflight` only verifies review approval plus raw-ingestion / parser-sandbox / redaction / size-budget gates while keeping `diff_executor_implemented=false`, `heap-snapshot-diff-executor-approval-plan` only plans approval record / transaction journal / bounded gate / result artifact contracts while keeping `approval_recorded=false`, `journal_written_now=false`, and `executor_invoked=false`, `heap-snapshot-diff-executor-approval-record` only records explicit reviewer approval metadata while keeping executor inputs inert, and `heap-snapshot-diff-executor-transaction-preflight` only verifies the ready approval plan plus written approval record for a future journal-writer review while keeping `transaction_started=false`, `journal_written=false`, `bounded_executor_gate_written=false`, `executor_invoked=false`, `raw_heap_loaded=false`, `raw_heap_parsed=false`, `raw_heap_exported=false`, and `heap_diff_computed=false`, and `record_heap_snapshot_diff_executor_transaction_journal` only writes reviewed transaction metadata while keeping `bounded_executor_gate_written=false`, `ready_to_execute_now=false`, `executor_invoked=false`, `raw_heap_loaded=false`, `raw_heap_parsed=false`, `raw_heap_exported=false`, and `heap_diff_computed=false`; `heap-snapshot-diff-executor-bounded-gate` only verifies the written journal and future executor contract metadata while keeping `ready_to_execute_now=false`, `bounded_executor_gate_written=false`, `executor_invoked=false`, `raw_heap_loaded=false`, `raw_heap_parsed=false`, `raw_heap_exported=false`, and `heap_diff_computed=false`; `heap-snapshot-diff-executor-result` can parse caller-provided V8 heap snapshot JSON objects and compute bounded summary deltas after explicit review approval while still exporting no raw heap / raw strings and making no complete traversal claims; `heap-snapshot-diff-followup-checkpoint` can review that result and recommend constructor drilldown, retained-size planning, path-to-root planning, or larger-budget review without loading / parsing raw heap or computing a new diff; `heap-snapshot-diff-selected-analysis-input-preflight` can select one recommended follow-up action and candidate input while keeping future executors unimplemented / uninvoked; `heap-snapshot-constructor-growth-drilldown` can summarize constructor growth candidates from a ready `review_constructor_growth` selected input and recommend retained-size / path-to-root preflight review while keeping real drilldown execution, retained-size proof, path-to-root computation, raw heap loading / export, new diff computation, larger-budget second pass, and automatic follow-up disabled; `heap-snapshot-retained-path-preflight` can package constructor-growth candidate inputs, raw heap requirements, and future retained-size / path-to-root executor contracts while keeping those future executors `implemented=false`, `raw_heap_loaded=false`, `raw_heap_parsed=false`, `raw_heap_exported=false`, `raw_strings_exported=false`, `heap_diff_computed=false`, `retained_size_proven=false`, and `path_to_root_computed=false`; `heap-snapshot-retained-size-input-review` can package retained-size candidate inputs, raw heap requirements, a disabled retained-size executor input contract, and approval gate metadata while keeping `executor_input_contract.implemented=false`, `approval_gate.ready_to_execute_now=false`, `raw_heap_loaded=false`, `raw_heap_parsed=false`, `raw_heap_exported=false`, `raw_strings_exported=false`, `heap_diff_computed=false`, and `retained_size_proven=false`; `heap-snapshot-retained-size-approval-plan` can plan approval-record, transaction-journal, bounded-gate, result-artifact, candidate digest, and disabled executor-input contract metadata while keeping `approval_recorded=false`, `transaction_started=false`, `journal_written_now=false`, `bounded_executor_gate_written=false`, `executor_invoked=false`, `raw_heap_loaded=false`, `raw_heap_parsed=false`, `raw_heap_exported=false`, `raw_strings_exported=false`, `heap_diff_computed=false`, `retained_size_proven=false`, and `path_to_root_computed=false`; `record_heap_snapshot_retained_size_approval` writes only `workspace/heap-snapshot-retained-size-approval-record.json` under explicit apply gates while keeping transaction / journal / bounded-gate / executor / raw-heap / retained-size / path-to-root effects false; `heap-snapshot-retained-size-transaction-preflight` consumes a ready approval plan plus written approved approval record and emits `workspace/heap-snapshot-retained-size-transaction-preflight.json` / `virtual://workspace/heap-snapshot-retained-size-transaction-preflight.json` as a read-only journal-writer review gate while keeping `transaction_started=false`, `journal_written=false`, `bounded_executor_gate_written=false`, `executor_invoked=false`, `raw_heap_loaded=false`, `raw_heap_parsed=false`, `raw_heap_exported=false`, `retained_size_proven=false`, and `path_to_root_computed=false`; `record_heap_snapshot_retained_size_transaction_journal` writes only `workspace/heap-snapshot-retained-size-executor-journal.json` under explicit apply gates and may mark transaction / journal audit metadata while keeping bounded-gate / executor / raw-heap / retained-size / path-to-root effects false; `heap-snapshot-retained-size-bounded-gate` consumes that written journal and emits `workspace/heap-snapshot-retained-size-bounded-gate.json` as a read-only executor review gate while keeping `ready_to_execute_now=false`, `executor_invoked=false`, `raw_heap_loaded=false`, `raw_heap_parsed=false`, `raw_heap_exported=false`, `retained_size_proven=false`, and `path_to_root_computed=false`; full heap graph diff semantics such as retained-size proof execution, path-to-root proof execution, raw heap file ingestion / export, raw-heap constructor drilldown proof, larger-budget second-pass execution, and automatic follow-up execution remain unsupported; `heap-snapshot-automatic-followup-plan` is available only as a read-only manual planner over existing estimate descriptors; `heap-snapshot-retained-size-proof-plan` is available only as a read-only retained-size proof planning descriptor over an existing estimate plus optional follow-up plan, with raw-heap / dominator-tree requirements and a disabled future proof executor contract; `heap-snapshot-path-to-root-proof-plan` is available only as a read-only path-to-root proof planning descriptor over an existing path estimate plus optional proof-plan context, with root-set / incoming-edge-walk requirements and a disabled future proof executor contract; the retained-size executor MVP now provides only explicit-review, caller-provided-snapshot, redacted candidate estimates with no retained-size proof; the path-to-root executor MVP now provides only explicit-review, caller-provided-snapshot, redacted bounded incoming-edge path estimates with no reachability proof; and the constructor-growth drilldown analysis MVP now provides only explicit-review, descriptor-backed candidate prioritization with no raw-heap constructor reachability proof.

### Strategy and rebuild quality

- Additional evidence scoring consumers beyond the current review-gate baseline if downstream automation needs richer policy tuning or external CI / delivery integration.
- Real third-party StrategyDetector provider packages beyond the template and reference provider baseline if site-specific or private detector corpora need to move out of core.

### Workspace and artifact evolution

- Use `reverse-agent-workspace-dual-write-smoke` / `review_workspace_dual_write_pilot_workflow` evidence from reviewed scoped dual-write runs before any broader dual-write rollout or foldered-canonical migration pilot; current smoke and workflow intentionally keep high-risk delivery / transaction artifacts out of default pilots and keep foldered-canonical migration blocked while partial consumers or `source_path` usage remain.
- Use reviewed `workspace-dual-write-expansion-result` evidence from explicit scoped expansion runs before any foldered-canonical migration pilot; the verifier now records observed output compatibility, but physical canonical-path migration remains separate.
- Run `execute_workspace_foldered_canonical_physical_apply` only after a ready physical-apply preflight, matching manifest dry-run / apply plan, current backend manifest evidence, explicit review approval, and `approve_physical_apply=true`; current support writes rollback checkpoint / append-only journal / result evidence and promotes backend manifest canonical paths; post-apply validation result recording, legacy fallback tightening readiness review, review-only tightening apply planning, read-only tightening executor preflight, explicit-review-only tightening executor, read-only finalization readiness review, review-only finalization planning, read-only finalization preflight review, explicit-review-only finalization executor, and read-only post-finalization audit, read-only broader rollout readiness, review-only broader rollout planning, read-only broader rollout preflight, explicit-review-only broader rollout metadata apply, and read-only broader rollout post-audit are now available. Finalization apply writes only workspace alias metadata, append-only journal, result artifact, and backend manifest metadata after explicit review approval; broader rollout apply likewise writes only workspace alias broader rollout metadata, append-only journal, result artifact, and backend manifest metadata after explicit review approval. Post-rollout audit, review-only rollback-vs-commit decision planning, explicit reviewed decision recording, explicit reviewed commit execution, read-only rollback preflight, and explicit reviewed rollback execution are now available.

### Delivery and external integration hardening

- Broader durable resume scheduler semantics beyond read-only readiness / dependency / evidence projection.
- Broader physical rollback state machine beyond local manifest rollback apply.
- Additional external distributed lock providers beyond local-file / SQLite / Redis where deployment needs justify them.
- Real third-party ExternalDeliveryProvider plugins beyond the template, S3-compatible baseline, GitLab Release record / binary asset upload baseline, and internal artifact registry baseline, such as provider-specific package registries.
- Advanced adaptive provider retry policy, retry budgets, and provider-specific rate-limit behavior.
- More complete external delivery partial-failure recovery.

## Explicitly deferred

These items are intentionally not part of the current Web-first execution track:

- Android full runtime chain, Frida workflows, APK / dex / JNI deep analysis, and device CI.
- iOS full runtime chain, Frida / LLDB workflows, IPA / Mach-O deep analysis, signing-dependent workflows, and device CI.
- Mini-program full runtime chain, private package extraction, account-bound developer-tool sessions, and bridge-bound signing workflows.
- Automatic lease-renewal daemon or polling loop.
- Automatic lock lifecycle manager.
- Automatic stale lock takeover.
- Redlock quorum consensus.
- Unreviewed automatic full cross-request materialization.
- Automatic rollback-vs-commit decision making.
- Automatic external delivery publication without explicit review / apply intent.

## Validation posture

- Public CI should stay focused on mock, pure-Python, and side-effect-free paths; BrowserProvider smoke policy coverage uses fixed synthetic fixture evidence under `tests/fixtures/browser-provider-smoke/` and must not launch browsers or call MCP.
- Real browser, CloakBrowser, remote CDP, legacy MCP, Redis, object storage, or GitHub Release smoke tests must remain explicit local or self-hosted checks.
- Provider metadata listing, doctor matrix output, registry discovery, and workspace contract export must stay side-effect-free.
